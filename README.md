# C.C calcu :3

A basic scientific calculator styled after a Casio fx-991EX, built with plain HTML, CSS, and JavaScript.

## Files

- `index.html` — page structure and button layout
- `style.css` — calculator body, screen, and button styling
- `script.js` — expression handling, math logic, and keyboard input

## Running it

Open `index.html` in a browser. All three files need to stay in the same folder since `index.html` links to `style.css` and `script.js`.

## Features

- Basic operations: add, subtract, multiply, divide, mod
- Scientific functions: sin, cos, tan, log, ln, square root, x^y, pi
- Parentheses support for grouped expressions
- Auto-switches to scientific notation for very large or very small results
- AC (clear) and DEL (backspace) buttons
- Keyboard support: number keys, `+ - * / ( ) %`, Enter to calculate, Backspace to delete, Escape to clear

## Notes

This is a for-fun front-end build, not an exact replica of the real fx-991EX firmware or feature set.
